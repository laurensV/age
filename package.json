{
  "name": "age",
  "version": "1.0.0",
  "private": true,
  "author": "Laurens Verspeek",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "core-js": "^3.8.3",
    "nuxt": "^2.14.12"
  },
  "devDependencies": {
    "node-sass": "^5.0.0",
    "push-dir": "^0.4.1",
    "sass-loader": "^10.1.1"
  }
}
